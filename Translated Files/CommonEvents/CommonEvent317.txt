CommonEvent 317
Name = "データ解析初回"

  999([317])
  ConditionalBranch([0, 2113, 1])
    ControlSwitches([2113, 2113, 0])
    ShowTextAttributes(["stein_fc2", 0, 0, 2])
    ShowText(["\\n<Promestein>Hold on a second. Let me check out this terminal. After messing around every chance I've had so far, I think I've gotten the hang of how to use these."])
    ShowText([""])
    ShowText([""])
    ShowTextAttributes(["stein_fc2", 2, 0, 2])
    ShowText(["\\n<Promestein>Let's see... Wow, there's a lot of data. It'll take a while just to scroll through it all..."])
    ShowText([""])
    ShowText([""])
    ShowTextAttributes(["brunhild_fc1", 0, 0, 2])
    ShowText(["\\n<Hild>There appears to be a large quantity of text."])
    ShowText([""])
    ShowTextAttributes(["ruka_fc1", 0, 0, 2])
    ShowText(["\\n<Luka>All those sentences are disappearing from the screen as fast as they appear. Are you actually reading that fast, Promestein?"])
    ShowText([""])
    ShowText([""])
    ShowTextAttributes(["stein_fc2", 0, 0, 2])
    ShowText(["\\n<Promestein>I was an angel librarian. Speed reading like this is nothing to me... All right, I think I've got the gist of it."])
    ShowText([""])
    ShowText([""])
    ShowTextAttributes(["stein_fc2", 0, 0, 2])
    ShowText(["\\n<Promestein>First of all, this facility's network is cut off. It appears that we can't read all the data from here."])
    ShowText([""])
    ShowText([""])
    ConditionalBranch([0, 4, 0])
      ShowTextAttributes(["alice_fc5", 0, 0, 2])
      ShowText(["\\n<Alice>So we'll need to look for more of these machines around here? What a pain..."])
      ShowText([""])
      ShowText([""])
      Empty([])

    BranchEnd([])
    ConditionalBranch([0, 5, 0])
      ShowTextAttributes(["iriasu_fc4", 2, 0, 2])
      ShowText(["\\n<Ilias>So we have to locate more computer terminals around here? Sounds like a waste of our time..."])
      ShowText([""])
      ShowText([""])
      Empty([])

    BranchEnd([])
    ExitEventProcessing([])
    Empty([])

  Else([])
    ShowTextAttributes(["stein_fc2", 0, 0, 2])
    ShowText(["\\n<Promestein>Let's check what's on this terminal. Hmmm..."])
    ShowText([""])
    ShowText([""])
    ShowTextAttributes(["", 0, 0, 2])
    ShowText(["Promestein quickly reads the text scrolling across the screen."])
    ExitEventProcessing([])
    Empty([])

  BranchEnd([])
  Empty([])
