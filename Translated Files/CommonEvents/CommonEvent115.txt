CommonEvent 115
Name = "メダル判定用"

  999([0])
  Comment(["買い物総額"])
  ConditionalBranch([12, "$game_library.purchase_gold >= 1"])
    Script(["gain_medal(1001)"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_library.purchase_gold >= 30000"])
    Script(["gain_medal(1002)"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_library.purchase_gold >= 300000"])
    Script(["gain_medal(1003)"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_library.purchase_gold >= 3000000"])
    Script(["gain_medal(1004)"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_library.purchase_gold >= 30000000"])
    Script(["gain_medal(1005)"])
    Empty([])

  BranchEnd([])
  Comment(["鍛冶回数"])
  ConditionalBranch([12, "$game_library.party_synthesize >= 1"])
    Script(["gain_medal(1011)"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_library.party_synthesize >= 50"])
    Script(["gain_medal(1012)"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_library.party_synthesize >= 200"])
    Script(["gain_medal(1013)"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_library.party_synthesize >= 500"])
    Script(["gain_medal(1014)"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_library.party_synthesize >= 1000"])
    Script(["gain_medal(1015)"])
    Empty([])

  BranchEnd([])
  Comment(["転職回数"])
  ConditionalBranch([12, "$game_library.party_class_change >= 1"])
    Script(["gain_medal(1021)"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_library.party_class_change >= 10"])
    Script(["gain_medal(1022)"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_library.party_class_change >= 100"])
    Script(["gain_medal(1023)"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_library.party_class_change >= 400"])
    Script(["gain_medal(1024)"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_library.party_class_change >= 1000"])
    Script(["gain_medal(1025)"])
    Empty([])

  BranchEnd([])
  Comment(["転種回数"])
  ConditionalBranch([12, "$game_library.party_tribe_change >= 1"])
    Script(["gain_medal(1031)"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_library.party_tribe_change >= 5"])
    Script(["gain_medal(1032)"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_library.party_tribe_change >= 50"])
    Script(["gain_medal(1033)"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_library.party_tribe_change >= 200"])
    Script(["gain_medal(1034)"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_library.party_tribe_change >= 500"])
    Script(["gain_medal(1035)"])
    Empty([])

  BranchEnd([])
  Comment(["戦闘回数"])
  ConditionalBranch([12, "$game_library.party_battle >= 1"])
    Script(["gain_medal(1041)"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_library.party_battle >= 300"])
    Script(["gain_medal(1042)"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_library.party_battle >= 2000"])
    Script(["gain_medal(1043)"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_library.party_battle >= 5000"])
    Script(["gain_medal(1044)"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_library.party_battle >= 10000"])
    Script(["gain_medal(1045)"])
    Empty([])

  BranchEnd([])
  Comment(["逃亡回数"])
  ConditionalBranch([12, "$game_library.party_escape >= 1"])
    Script(["gain_medal(1051)"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_library.party_escape >= 10"])
    Script(["gain_medal(1052)"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_library.party_escape >= 50"])
    Script(["gain_medal(1053)"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_library.party_escape >= 150"])
    Script(["gain_medal(1054)"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_library.party_escape >= 500"])
    Script(["gain_medal(1055)"])
    Empty([])

  BranchEnd([])
  Comment(["敗北回数"])
  ConditionalBranch([12, "$game_library.party_lose >= 1"])
    Script(["gain_medal(1061)"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_library.party_lose >= 30"])
    Script(["gain_medal(1062)"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_library.party_lose >= 100"])
    Script(["gain_medal(1063)"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_library.party_lose >= 300"])
    Script(["gain_medal(1064)"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_library.party_lose >= 500"])
    Script(["gain_medal(1065)"])
    Empty([])

  BranchEnd([])
  Comment(["撃破数"])
  ConditionalBranch([12, "$game_library.party_defeat >= 500"])
    Script(["gain_medal(1071)"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_library.party_defeat >= 5000"])
    Script(["gain_medal(1072)"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_library.party_defeat >= 10000"])
    Script(["gain_medal(1073)"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_library.party_defeat >= 30000"])
    Script(["gain_medal(1074)"])
    Empty([])

  BranchEnd([])
  Comment(["職業図鑑解放率"])
  ConditionalBranch([12, "$game_library.party_open_class == 1"])
    Script(["gain_medal(1201)"])
    Empty([])

  BranchEnd([])
  Comment(["種族図鑑解放率"])
  ConditionalBranch([12, "$game_library.party_open_tribe == 1"])
    Script(["gain_medal(1202)"])
    Empty([])

  BranchEnd([])
  Comment(["アクター図鑑解放率"])
  ConditionalBranch([12, "$data_library.collect_per_actor == 1"])
    Script(["gain_medal(1301)"])
    Empty([])

  BranchEnd([])
  Comment(["エネミー図鑑解放率"])
  ConditionalBranch([12, "$data_library.collect_per_enemy == 1"])
    Script(["gain_medal(1311)"])
    Empty([])

  BranchEnd([])
  Comment(["武器図鑑解放率"])
  ConditionalBranch([12, "$data_library.collect_per_weapon == 1"])
    Script(["gain_medal(1321)"])
    Empty([])

  BranchEnd([])
  Comment(["防具図鑑解放率"])
  ConditionalBranch([12, "$data_library.collect_per_armor == 1"])
    Script(["gain_medal(1331)"])
    Empty([])

  BranchEnd([])
  Comment(["アクセサリー図鑑解放率"])
  ConditionalBranch([12, "$data_library.collect_per_accessory == 1"])
    Script(["gain_medal(1341)"])
    Empty([])

  BranchEnd([])
  Comment(["アイテム図鑑解放率"])
  ConditionalBranch([12, "$data_library.collect_per_item == 1"])
    Script(["gain_medal(1351)"])
    Empty([])

  BranchEnd([])
  Comment(["全体収集率"])
  ConditionalBranch([12, "$data_library.collect_per_all == 1"])
    Script(["gain_medal(1371)"])
    Empty([])

  BranchEnd([])
  CallCommonEvent([9138])
  ConditionalBranch([1, 14, 0, 32, 0])
    Script(["gain_medal(2151)"])
    Empty([])

  BranchEnd([])
  ControlVariables([14, 14, 0, 0, 0])
  Empty([])
