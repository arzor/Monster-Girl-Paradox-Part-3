CommonEvent 7191
Name = "Orlan: Talk"

  999([7191])
  ConditionalBranch([1, 820, 0, 0, 0])
    ControlVariables([820, 820, 0, 0, 1])
    Empty([])

  BranchEnd([])
  PlayBGM(["RPG::BGM(@name=\"ero2\", @volume=75, @pitch=100)"])
  ConditionalBranch([1, 1143, 0, 10, 2])
    ShowTextAttributes(["mob_bukiya1_fc1", 0, 0, 2])
    ShowText(["\\n<Orlan>A battle fuck with me?\nInteresting. Been a while since a guy has challenged me."])
    ShowText([""])
    ShowText([""])
    Empty([])

  BranchEnd([])
  ConditionalBranch([1, 1143, 0, 11, 1])
    ShowTextAttributes(["mob_bukiya1_fc1", 0, 0, 2])
    ShowText(["\\n<Orlan>You want to battle fuck with me...?\nThe world's a mess, yet you're still in high spirits."])
    ShowText([""])
    ShowText([""])
    Empty([])

  BranchEnd([])
  ConditionalBranch([1, 820, 0, 2, 1])
    ShowTextAttributes(["mob_bukiya1_fc1", 0, 0, 2])
    ShowText(["\\n<Orlan>Bet's 350G. If I lose, I got nothing for ya.\nYou already won once, ya know?"])
    ShowText([""])
    ShowText([""])
    Empty([])

  Else([])
    ShowTextAttributes(["mob_bukiya1_fc1", 0, 0, 2])
    ShowText(["\\n<Orlan>Bet's 350G. If I lose, I'll give ya an accessory.\nIt's a worthwhile item that reduces the SP consumption of your skills. Pretty handy if you like fighting with lots of special moves."])
    ShowText([""])
    ShowText([""])
    Empty([])

  BranchEnd([])
  ShowTextAttributes(["mob_bukiya1_fc1", 0, 0, 2])
  ShowText(["\\n<Orlan>Just for your information, I'm from an Assassin family.\nWe're trained in a lot of sexual skills, ya know?"])
  ShowText([""])
  ShowText([""])
  ShowChoices([["Battle Fuck Challenge", "Reconsider"], 2])
  When([0, "バトルファックを挑む"])
    ConditionalBranch([7, 350, 0])
      ConditionalBranch([0, 2331, 1])
        ChangePlayerFollowers([1])
        ShowTextAttributes(["", 0, 0, 2])
        ShowText(["Luka's friends left the building..."])
        Empty([])

      BranchEnd([])
      CallCommonEvent([7192])
      ExitEventProcessing([])
      Empty([])

    Else([])
      ShowTextAttributes(["mob_bukiya1_fc1", 0, 0, 2])
      ShowText(["\\n<Orlan>Oi, oi, you don't have the money.\nDon't get carried away with yourself..."])
      ShowText([""])
      ShowText([""])
      ConditionalBranch([0, 443, 0])
        ExitEventProcessing([])
        Empty([])

      BranchEnd([])
      ConditionalBranch([0, 2331, 1])
        ErasePicture([5])
        PlayBGM(["RPG::BGM(@name=\"city1\", @volume=100, @pitch=100)"])
        Empty([])

      BranchEnd([])
      ExitEventProcessing([])
      Empty([])

    BranchEnd([])
    Empty([])

  When([1, "やめておく"])
    ShowTextAttributes(["mob_bukiya1_fc1", 0, 0, 2])
    ShowText(["\\n<Orlan>Running away, eh?\nThought I'd be able to squeeze someone for the first time in a while, too..."])
    ShowText([""])
    ShowText([""])
    ConditionalBranch([0, 443, 0])
      ExitEventProcessing([])
      Empty([])

    BranchEnd([])
    ConditionalBranch([0, 2331, 1])
      ErasePicture([5])
      PlayBGM(["RPG::BGM(@name=\"city1\", @volume=100, @pitch=100)"])
      Empty([])

    BranchEnd([])
    ExitEventProcessing([])
    Empty([])

  ChoicesEnd([])
  Empty([])
