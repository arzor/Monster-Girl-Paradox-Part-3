CommonEvent 6
Name = "敵会話・誘惑判定"

  999([6])
  ConditionalBranch([0, 41, 0])
    ExitEventProcessing([])
    Empty([])

  BranchEnd([])
  ConditionalBranch([0, 86, 0])
    ExitEventProcessing([])
    Empty([])

  BranchEnd([])
  CallCommonEvent([201])
  Comment(["スイッチ11がオフなら、ルカが有効ではないのでイベント中断"])
  CommentMore(["以降の誘惑と会話はスキップ"])
  ConditionalBranch([0, 11, 1])
    ExitEventProcessing([])
    Empty([])

  BranchEnd([])
  Comment(["スイッチ12は「誘惑か会話が1度も行われていないか」を表す"])
  CommentMore(["コモン203,204で誘惑か会話を行う時にスイッチ12をオフにする"])
  CommentMore(["スイッチ12がオフになると、以降の誘惑と会話はスキップする"])
  ControlSwitches([12, 12, 0])
  ControlVariables([13, 13, 0, 2, 1, 4])
  ConditionalBranch([12, "all_members_armor_have?(1740)"])
    ControlVariables([13, 13, 0, 0, 1])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "all_members_armor_have?(1741)"])
    ControlVariables([13, 13, 0, 0, 1])
    Empty([])

  BranchEnd([])
  ConditionalBranch([0, 22, 0])
    ControlVariables([13, 13, 0, 0, 1])
    Empty([])

  BranchEnd([])
  ConditionalBranch([0, 21, 0])
    ControlVariables([13, 13, 0, 0, 0])
    Empty([])

  BranchEnd([])
  ConditionalBranch([1, 13, 0, 1, 0])
    ControlVariables([112, 112, 0, 0, 0])
    Loop([])
      ControlVariables([112, 112, 1, 0, 1])
      Comment(["誘惑の判定および実行"])
      CallCommonEvent([203])
      Comment(["スイッチ12がオフなら、誘惑が行われたのでイベント中断"])
      CommentMore(["以降の誘惑と会話はスキップ"])
      ConditionalBranch([0, 12, 1])
        Comment(["■■■必ず変数112を0にすること■■■"])
        ControlVariables([112, 112, 0, 0, 0])
        ExitEventProcessing([])
        Empty([])

      BranchEnd([])
      Comment(["６の実行後にループ中断"])
      ConditionalBranch([1, 112, 0, 6, 0])
        BreakLoop([])
        Empty([])

      BranchEnd([])
      Empty([])

    RepeatAbove([])
    Empty([])

  BranchEnd([])
  Label(["会話判定"])
  ConditionalBranch([1, 29, 0, 143, 0])
    Comment(["■■■必ず変数112を0にすること■■■"])
    ControlVariables([112, 112, 0, 0, 0])
    ExitEventProcessing([])
    Empty([])

  BranchEnd([])
  ControlVariables([13, 13, 0, 2, 1, 20])
  ConditionalBranch([1, 13, 0, 1, 0])
    ControlVariables([112, 112, 0, 0, 0])
    Loop([])
      ControlVariables([112, 112, 1, 0, 1])
      Comment(["会話の判定および実行"])
      CallCommonEvent([204])
      Comment(["スイッチ12がオフなら、会話が行われたのでイベント中断"])
      CommentMore(["以降の会話はスキップ"])
      ConditionalBranch([0, 12, 1])
        Comment(["■■■必ず変数112を0にすること■■■"])
        ControlVariables([112, 112, 0, 0, 0])
        ExitEventProcessing([])
        Empty([])

      BranchEnd([])
      Comment(["６の実行後にループ中断"])
      ConditionalBranch([1, 112, 0, 6, 0])
        BreakLoop([])
        Empty([])

      BranchEnd([])
      Empty([])

    RepeatAbove([])
    Empty([])

  BranchEnd([])
  Comment(["■■■必ず変数112を0にすること■■■"])
  ControlVariables([112, 112, 0, 0, 0])
  Empty([])
